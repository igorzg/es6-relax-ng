import {ApplyFlip} from '../src/class/apply-flip';

describe('ApplyFlip', function() {
  it('Should create an instance', function() {
       var ob = new ApplyFlip();
       console.log(ob);
  });

});


